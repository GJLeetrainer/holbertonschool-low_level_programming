#include <stdio.h>

/**
 * main - entry point
 * @main - Print every single digit base 10 number
 * Return: Always 0
**/
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
	printf("%d", i);
	}
	printf("\n");
	return (0);
}
